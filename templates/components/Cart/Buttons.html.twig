{# <twig:c975LShop:Cart:Buttons cart="{{ cart }}"/> #}
{# Delete Link #}
{% if cart.total > 0 %}
    <twig:c975LShop:Cart:DeleteLink/>
{% endif %}
{# Continue shopping #}
<twig:c975LSite:Button:Button type="secondary" label="Poursuivre les achats" url="{{ path('shop_index') }}#products"/>
{# Validate button #}
{% if cart.total > 0 %}
    <twig:c975LSite:Button:Button label="Valider le panier" url="{{ app.request.uri }}#validate"/>
{% endif %}