<section id="author">
    <h2>{{ 'label.author'|trans({}, 'shop') }}</h2>
    <h3 class="text"><strong>{{ name }}</strong></h3>
    {{ presentation|raw }}
    {% if null != website %}
        <p><a href="{{website }}" target="_blank">{{ website }}</a></p>
    {% endif %}
</section>