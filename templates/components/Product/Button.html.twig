{% if product is not defined and this.product is defined %}
    {% set product = this.product %}
{% endif %}
<twig:c975LUi:Button:Button type="{{ type|default('primary') }}" label="{{ product.title }}" url="{{ url('product_display', {'slug': product.slug}) }}"/>
