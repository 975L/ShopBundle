{# Kept as the name the sites overriding it already call, the bundle's own pages no longer emitting it: PaymentBundle documents its bar as placed once in the site's layout, and a page adding a second one leaves the second frozen #}
<twig:c975LPayment:Basket:Navbar/>
