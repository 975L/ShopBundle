{# The bar the basket controller of PaymentBundle shows as soon as the basket holds something - the button carries the count and the total it keeps up to date, so nothing is repeated around it #}
<div id="basket-navbar" class="basket-navbar d-none">
    <twig:c975LPayment:Basket:ViewButton type="secondary" inline="true"/>
</div>
