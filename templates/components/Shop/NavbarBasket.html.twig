{# Kept as the name the sites overriding it already call, the bundle's own pages no longer emitting it: UiBundle's layout places PaymentBundle's bar once on every page, and a page adding a second one leaves the second frozen #}
<twig:c975LPayment:Basket:Navbar/>
